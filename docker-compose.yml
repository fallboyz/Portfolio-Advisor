services:
  portfolio-advisor:
    build: .
    ports:
      - "20012:8501"
      - "20013:8001"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml
    env_file:
      - .env
    environment:
      - TZ=Asia/Seoul
    restart: unless-stopped
